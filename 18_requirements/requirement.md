### requirement.txt

untuk memebuat file requirement.txt dari package yang sudah terinstall di project
kita bisa menggunakan perintah:
```sh
pip freeze > requirement.txt
```

menginstall semua package di file requests.txt
```sh
pip install -r requirements.txt 
```